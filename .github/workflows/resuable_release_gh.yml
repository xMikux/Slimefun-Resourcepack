name: GitHub Release

on:
  workflow_call:
    inputs:
      release:
        type: boolean
        required: false

jobs:
  github_release:
    name: Release GitHub
    runs-on: ubuntu-latest
    steps:
      - name: abc
        run: |
          abc